apiVersion: 1

providers:
  - name: claude-code
    orgId: 1
    folder: "Claude Code"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
